# 實驗已完成

請閱讀 README 格式的中文 [完整報告](README.md)。

- 完成 231 組參數、每組 1,000 次，共 231,000 次獨立模擬。
- 已保存完整樣本、種子、統計 CSV、PNG 與 PDF 圖表。
- 5 項自動測試、續跑修復整合檢查、全量資料核對及圖表排版檢查均通過。
- 全部 231,000 個種子唯一；已修正種子碰撞與浮點網格問題，受影響資料已重新計算。

## 以下為執行過程紀錄

已啟動完整實驗：每組參數 1,000 次獨立模擬，預期互動長度 1–10 與 100，錯誤率 0–0.5、間距 0.025，共 231 組、231,000 次模擬。每次維持原模型的 36 個個體、100 代、每代淘汰並複製 3 個個體。

已新增可重現的加速程式與驗證測試；4 項測試全部通過，涵蓋策略歷史、原始配對結果、人口守恆、固定種子重現，以及同分排序。

為了和原研究比較，保留原本的單向錯誤機制（合作可能變背叛）與整數亂數機率。曲線將使用實際樣本平均，不以平滑濾波改寫結果，並顯示平均值的蒙地卡羅不確定性。

完成後請閱讀 README 格式的 [實驗報告](README.md)。原始資料及每組模擬種子保存於 `results/high_sample/`。

補充：檢查舊資料後，`jsons/result.json` 實際每組合計 360 個個體，相當於 10 次模擬；`jsons/result_100.json` 為 30 次。因此本次分別提高到舊資料的 100 倍與約 33 倍。互動長度 1–7 的實驗已完成，其他條件持續執行中。

目前已完成長度 1–8，正在執行 9–10 與 100。新增的跨執行緒重現性檢查也已通過。初步結果顯示原結論需要更精確：互動長度 4 且錯誤率 0.5 時，背叛傾向策略仍然占多數，不能只根據互動長度判斷寬容策略是否占優。

長度 1–10 的 210,000 次模擬已完成，正在執行耗時較長的 100 輪條件。資料檢查發現某組參數產生重複種子，已加入自動去重及回歸測試，完成後將重跑受影響條件，確保每組 1,000 個不同的模擬種子。另新增 `--resume` 支援沿用已完成且參數一致的資料。

長度 100 的前幾個條件已完成：有錯誤時 Random 的優勢相當明顯，但零錯誤時不是如此。最後會同時檢查所有條件的種子唯一性，以及浮點數網格是否意外跨過原模型的整數機率門檻；受影響條件將重新計算後才產出報告。

目前長度 100 已算到錯誤率 0.275。README 格式中文報告的產生程式已完成，將包含主要發現、完整曲線與 95% 區間、30 次對照、策略及報酬表、模型限制與重現指令。5 項自動測試及續跑修復整合檢查均通過。

主模擬接近完成，接著會修正受影響的種子與錯誤率網格，重新驗證完整資料，再生成圖表並檢查排版。

主模擬已全部完成。修正重跑已完成長度 1–9；正在處理 10 與 100。最終圖表將使用修正後資料，原始研究檔案不會被覆寫。

修正重跑只剩長度 100 的後半錯誤率。下一步是逐檔核對人口總數、平均值、標準誤、區間與種子，並產出 `doc/README.md` 和圖表。
